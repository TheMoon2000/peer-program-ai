import { RoomInfo } from "@/Data Structures";

interface Props {
    roomInfo: RoomInfo;
}
  
/* Lobe Chat integration */
export function ChatPlaceholder(props: Props) {
    return <div className="w-full h-full">
        {/* <Chat></Chat> */}
    </div>
}